# Unified-System-Registry-DependencyGraph.ts
Deterministic dependency‑graph module for Beast System 3.0. Maps engine→engine dependencies from the Unified System Registry, validates structural integrity, detects cycles, and ensures authority‑safe, invariant‑aligned execution ordering across all system components.
